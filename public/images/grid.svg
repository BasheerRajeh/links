<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 32 32' width='32' height='32' fill='none'>
  <style>
    :root {
        --stroke-color: rgba(0, 0, 0, 0.1); /* Dark mode */
    }

    @media (prefers-color-scheme: dark) {
      :root {
      --stroke-color: rgba(255, 255, 255, 0.1); /* Default: Light mode */
      }
    }
  </style>
  <path d='M0 .5H31.5V32' stroke='var(--stroke-color)' />
</svg>
